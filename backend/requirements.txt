# ============================================================
# Sk6.0 Backend — Python Dependencies
# Pin all versions for reproducible builds.
# Update via: pip-compile requirements.in
# ============================================================

# ── Web Framework ──────────────────────────────────────────
fastapi==0.110.1
uvicorn[standard]==0.29.0
gunicorn==21.2.0
python-multipart==0.0.9        # Multipart form data (screenshot upload)
httpx==0.27.0                  # Async HTTP client (for testing + OTP gateway)

# ── Database ───────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.29
asyncpg==0.29.0                # Async PostgreSQL driver
alembic==1.13.1                # Schema migrations
psycopg2-binary==2.9.9         # Sync driver (used by Alembic CLI)

# ── Cache / Session ────────────────────────────────────────
redis[hiredis]==5.0.3          # Redis client with C parser for speed

# ── Background Jobs ────────────────────────────────────────
celery[redis]==5.3.6           # Task queue, Redis as broker + backend
flower==2.0.1                  # Celery monitoring web UI (internal only)

# ── Auth / Security ────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT encode/decode
passlib[bcrypt]==1.7.4             # Password hashing (bcrypt cost=12)
cryptography==42.0.5               # Backend for python-jose

# ── Validation ─────────────────────────────────────────────
pydantic==2.7.0
pydantic-settings==2.2.1       # Config from .env files

# ── Logging ────────────────────────────────────────────────
structlog==24.1.0              # Structured JSON logging

# ── Observability ──────────────────────────────────────────
prometheus-fastapi-instrumentator==6.1.0   # Auto-instrument FastAPI endpoints
opentelemetry-api==1.24.0      # Optional: OTEL tracing (future)

# ── File Handling ──────────────────────────────────────────
python-magic==0.4.27           # MIME type validation for screenshot uploads
Pillow==10.3.0                 # Image validation (check screenshot is real image)
reportlab==4.1.0               # PDF generation for admin reports

# ── External Services ──────────────────────────────────────
firebase-admin==6.5.0          # FCM push notifications

# ── Utilities ──────────────────────────────────────────────
python-dateutil==2.9.0         # Timezone-aware date handling
pytz==2024.1                   # Timezone: Asia/Kolkata
orjson==3.10.1                 # Fast JSON serialisation (FastAPI compatible)
tenacity==8.2.3                # Retry logic with exponential backoff
